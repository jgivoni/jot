"use strict";
$(function () {
    $.widget("replanner.createItemForm", {
        // default options
        options: {
        },
        // the constructor
        _create: function () {
        }
    });

    // initialize widget
    $("form#create-item").createItemForm();
});
